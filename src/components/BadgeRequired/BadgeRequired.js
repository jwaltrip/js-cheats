import React from 'react';
import { Badge } from 'reactstrap';

// this function will be used to color in a badge that will say required
// color should render in blue.
const BadgeRequired = () => <Badge color="primary">required</Badge>;

export default BadgeRequired;