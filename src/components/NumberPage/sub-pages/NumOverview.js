import React from 'react';

const NumOverview = () => {
  return (
    <div>
      <h1>Javascript Numbers Overview</h1>
      <hr/>
    
      <p>JavaScript has only one type of number.</p>
      <p>Numbers can be written with or without decimals.</p>
      <p>Unlike many other programming languages, JavaScript <i>does not</i> define different types of numbers, such as: <code>integers</code>, <code>short</code>, <code>long</code>, <code>floating-point</code> etc.</p>
      <p>JavaScript numbers are always stored as double precision floating point numbers, following the international IEEE 754 standard.</p>
    </div>
  );
};

export default NumOverview;